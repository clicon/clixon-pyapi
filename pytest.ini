[pytest]
addopts = -v --cov=clixon --cov=clixon_rest --capture=no
# Only the test suite, so that scratch files in the root of the repository are
# not collected and run.
testpaths = tests
python_files = test_*.py
